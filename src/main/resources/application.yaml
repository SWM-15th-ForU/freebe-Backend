spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${SPRING.DATASOURCE_URL}
    username: ${SPRING.DATASOURCE_USERNAME}
    password: ${SPRING.DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-name: Kakao
            client-id: ${KAKAO.CLIENT_ID}
            client-secret: ${KAKAO.CLIENT_SECRET}
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - account_email
              - name
              - birthday
              - birthyear
              - phone_number
          cognito:
            client-id: ${COGNITO.CLIENT_ID}
            client-secret: ${COGNITO.CLIENT_SECRET}
            client-name: ${COGNITO.CLIENT_NAME}
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
          cognito:
            issuer-uri: https://cognito-idp.${AWS.REGION}.amazonaws.com/${COGNITO.USER_POOL_ID}
            user-name-attribute: cognito:username
